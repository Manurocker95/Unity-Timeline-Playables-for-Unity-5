using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Playables;
using UnityEngine.Timeline;

[TrackColor(0.855f,0.8623f,0.870f)]
// Specifies the type of Playable Asset this track manages
// [TrackClipType(typeof(MyCustomPlayableAsset))]
// Use if the track requires a binding to a scene object or asset
// [TrackBindingType(typeof(GameObject))]
public class #SCRIPTNAME# : TrackAsset
{
	// override the type of mixer playable used by this track
	public override PlayableHandle CreateTrackMixer(PlayableGraph graph, GameObject go, int inputCount) {
		// 
		// return graph.CreateScriptMixerPlayable<MyCustomMixerPlayable>(inputCount);
		return base.CreateTrackMixer(graph, go, inputCount);
	}
}
